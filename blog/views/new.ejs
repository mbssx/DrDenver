<% include partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header">New Post</div>
    <form class="ui form" action="/blogs" method="POST">
        <div class="field">
            <label>Title</label>
            <input type="text" name="blogpost[title]">
        </div>
        <div class="field">
            <label>Image</label>
            <input type="text" name="blogpost[image]">
        </div>
        <div class="field">
            <label>Post Content</label>
            <textarea name="blogpost[body]"></textarea>
        </div>
        <button class="ui big blue basic button">Create Post</button>
    </form>
</div>
<% include partials/footer %>

<% include ./partials/footer %>